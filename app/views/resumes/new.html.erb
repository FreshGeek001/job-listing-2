<div class="col-md-4 col-md-offset-4">
<h3 class="text-center">提交简历到-<%= @job.title %></h3>

<hr>
<%= simple_form_for [@job, @resume] do |f| %>
<%= f.input :content%>
<%= f.input :attachment %>
<%= f.submit "Send", class:"btn btn-primary"%>
<% end %>
</div>
