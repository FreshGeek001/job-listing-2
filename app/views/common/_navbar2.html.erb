<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">
        <i class="fa fa-superpowers" aria-hidden="true"></i>iOffer</a>
    </div>
    <ul class="nav navbar-nav">
      <li><%= link_to("Jobs", jobs_path)%></li>
      <li><%= link_to("Admin", admin_jobs_path) %></li>
      <li><%= link_to("Add a job", new_admin_job_path)%></li>
    </ul>

    <!-- Collect the nav links, forms, and other contect for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if !current_user %>
        <li>
          <a href="/users/sign_up">
            <span class="fa fa-user-plus"></span>Sign up</a>
        </li>
        <li>
          <a href="/users/sign_in">
            <span class="fa fa-sign-in"></span>Sign in</a>
        </li>
      <% else %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">

            <i class="fa fa-envelope" aria-hidden="true"></i>
            Hi!,<%= current_user.email %>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <% if current_user.admin? %>
            <li>
              <%= link_to("Admin Panel", admin_jobs_path) %>
            </li>
            <% end %>
            <li>
              <%= link_to(content_tag(:i, '登出', class: 'fa fa-sign-out'), destroy_user_session_path, method: :delete) %>
            </li>
          </ul>
        </li>
        <% end %>
      </ul>
    </div>
    <!--/.navbar-collapse -->
  </div>
  <!--/.container-fluid -->
</nav>
