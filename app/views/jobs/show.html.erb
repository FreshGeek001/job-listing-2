<div class="text-center">
<h1><%= @job.title %></h1>

<p>
  <%= simple_format(@job.description) %>
  薪资范围
  <%= @job.wage_lower_bound%>
  ~
  <%= @job.wage_upper_bound%>
</p>
<%= link_to("提交简历", new_job_resume_path(@job), :style => "font-size: 15px;", class: "btn btn-info")%>
</div>
