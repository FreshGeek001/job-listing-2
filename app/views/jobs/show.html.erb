<h1><%= @job.title %></h1>

<p>
  <%= simple_format(@job.description) %>
  Location:
  <%= @job.location%>
  <br>
  Salary:
  <%= @job.wage_lower_bound%>
  ~
  <%= @job.wage_upper_bound%>
</p>
<%= link_to("Submit", new_job_resume_path(@job), :style => "font-size: 15px;", class: "btn btn-info")%>
